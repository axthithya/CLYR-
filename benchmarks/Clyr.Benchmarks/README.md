# Clyr.Benchmarks scope

Phase 2 will create the benchmark project for bounded scan aggregation, top-N, progress/cancellation, and device fixtures; later phases add rule, SQLite, IPC, and adapter scenarios. `docs/PERFORMANCE_BUDGETS.md` contains provisional budgets and the revision process.
