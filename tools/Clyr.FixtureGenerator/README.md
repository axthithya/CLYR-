# Clyr.FixtureGenerator scope

Planned for Phase 1: deterministic fictional metadata/tree generation with fixed seeds, bounded output, explicit destination containment, and no cleanup outside its verified temporary root. Filesystem-specific fixtures are created only on disposable test volumes.
