# Planned development tools

Repository tools must generate synthetic deterministic data and validate local artifacts; they never collect real user scans or execute community scripts. Phase 1 creates the fixture-generator skeleton after the .NET toolchain is pinned.
