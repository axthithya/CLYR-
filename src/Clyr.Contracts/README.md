# Clyr.Contracts boundary

Planned for Phase 1: minimal versioned DTOs, enums, schema/protocol versions, and strong identifiers shared only where an external or process boundary requires them. Contracts are bounded and serialization-tested; mutable services and policy implementations do not belong here.
