# Clyr.Core boundary

Planned for Phase 1: UI-independent domain/application abstractions and policies testable with fake filesystem, clock, persistence, process, and Windows-capability adapters. It may depend inward on `Clyr.Contracts`; it must not reference WinUI, concrete SQLite, Win32 implementations, or a generic command runner.
