<UserControl
    x:Class="Clyr.App.Controls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="HeaderGrid" ColumnSpacing="16" RowSpacing="12" SizeChanged="HeaderSizeChanged">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Spacing="6" MinWidth="0">
            <TextBlock Text="{x:Bind Title, Mode=OneWay}" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="{x:Bind Subtitle, Mode=OneWay}" Style="{StaticResource PageSubtitleStyle}" AutomationProperties.Name="Page subtitle" />
        </StackPanel>
        <Border x:Name="TrustBadge" Grid.Column="1" Style="{StaticResource TrustBadgeStyle}" VerticalAlignment="Top"
                AutomationProperties.Name="Private Local Read-only trust badge">
            <TextBlock Text="Private · Local · Read-only" FontSize="12" FontWeight="SemiBold" />
        </Border>
    </Grid>
</UserControl>
