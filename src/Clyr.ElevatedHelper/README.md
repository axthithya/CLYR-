# Clyr.ElevatedHelper boundary

This future Phase 6 executable is documented now but must not be created by Phase 1. It will be short-lived, signed/same-product identified, authenticated, versioned, capability-bound, batch-limited, and receipt-producing. It is not a service and exposes no arbitrary path, command, script, or generic process endpoint.
